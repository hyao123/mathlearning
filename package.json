{
  "name": "mathlearning",
  "version": "0.1.0",
  "private": true,
  "description": "本地可用的小学奥数学习系统。",
  "type": "commonjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node scripts/check-syntax.js"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  },
  "engines": {
    "node": ">=20"
  }
}
